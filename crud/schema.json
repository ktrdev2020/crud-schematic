
{
  "$schema": "http://json-schema.org/schema",
  "id": "CrudSchema",
  "title": "Crud Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the entity"
    },
    "model": {
      "type": "string",
      "description": "Model fields in 'name:type,name2:type2' format"
    }
  },
  "required": ["name", "model"]
}
