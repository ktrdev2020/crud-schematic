{
  "$schema": "http://json-schema.org/schema",
  "$id": "CrudSchema",
  "title": "Crud Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the entity"
    }
  },
  "required": ["name"]
}
